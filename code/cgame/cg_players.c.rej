***************
*** 3096,3103 ****
  	int				h_pistol = -1; //biggest wp-num of holstered pistols
  	qboolean		addguns = cg_addguns.integer;
  	
- 	qboolean	hide = qfalse;
- 
  	// the client number is stored in clientNum.  It can't be derived
  	// from the entity number, because a single client may have
  	// multiple corpses on the level using the same clientinfo
--- 3096,3101 ----
  	int				h_pistol = -1; //biggest wp-num of holstered pistols
  	qboolean		addguns = cg_addguns.integer;
  	
  	// the client number is stored in clientNum.  It can't be derived
  	// from the entity number, because a single client may have
  	// multiple corpses on the level using the same clientinfo
***************
*** 3113,3128 ****
  	if ( !ci->infoValid ) {
  		return;
  	}
- 
- 	//if player isn't visible, don't show it
- 	/*if(cent->currentState.clientNum != cg.snap->ps.clientNum && !(cent->currentState.eFlags & EF_DEAD)){
- 
- 		hide = CG_CheckPlayerVisible(cg.refdef.vieworg, cent->lerpOrigin);
- 
- 	}*/
- 
- 	/*if(ci->team >= TEAM_SPECTATOR)
- 		return;*/
  
  	// get the player model information
  	renderfx = 0;
--- 3111,3116 ----
  	if ( !ci->infoValid ) {
  		return;
  	}
  
  	// get the player model information
  	renderfx = 0;
***************
*** 3179,3193 ****
  	CG_PlayerSplash( cent );
  
  	// use the same origin for all
- #ifdef MISSIONPACK
  	if( cgs.gametype == GT_HARVESTER ) {
  		CG_PlayerTokens( cent, renderfx );
  	}
  #endif
- 
- 	//hide player if not visible
- 	if(hide)
- 		return;
  
  	//
  	// add the legs
--- 3167,3177 ----
  	CG_PlayerSplash( cent );
  
  	// use the same origin for all
+ #ifndef MISSIONPACK
  	if( cgs.gametype == GT_HARVESTER ) {
  		CG_PlayerTokens( cent, renderfx );
  	}
  #endif
  
  	//
  	// add the legs
